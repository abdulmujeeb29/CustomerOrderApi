@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Home Page";
}

<h2>Welcome to the Authentication Page</h2>

<p>Select an option below to proceed:</p>

<!-- Login Button -->

<p>
    <a href="@Url.Action("Login", "Authentication")" class="btn btn-primary">Login</a>
</p>

<!-- Register Button -->
<p>
    <a href="@Url.Action("Register", "Authentication")" class="btn btn-primary">Register</a>
</p>

<!-- Logout Button -->
<p>
    <a href="@Url.Action("Logout", "Authentication")" class="btn btn-secondary">Logout</a>
</p>

<!-- API Endpoints Button (Link to Swagger UI) -->
<p>
    <a href="/swagger" class="btn btn-info">API Endpoints</a>
</p>

